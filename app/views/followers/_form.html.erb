<div class="container-fluid">
  <h1 class="h3 mb-2 text-gray-800"> Cadastrar Seguidor </h1>
    <div class="card shadow mb-4">
      <div class="card-body">

        <%= simple_form_for follower do |c| %>
            <div class="row">
              <div class="col-md-6">
                <%= c.input :name, label: "Nome do Seguidor"%>
              </div>

              <div class="col-md-2">
                <%= c.input :birth, discard_year: true, order: [:day, :month], label: "Aniversário"%>
              </div>

              <div class="col-md-4">
                <%= c.association :parish, label_method: :name, include_blank: false, value_method: :id, label: "Paróquia"%>
              </div>
            </div>

          <div class="row">
            <div class="col-md-6">
              <%= c.input :address, label: "Endereço" %>
            </div>

            <div class="col-md-4">
              <%= c.input :email, label: "Email" %>
            </div>  

            <div class="col-md-2">
              <%= c.input :contact, label: "Contato" %>
            </div> 
          </div>

          <%= c.button :submit, "Cadastrar Seguidor", class: "btn btn-primary mt-3"%>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= render "layouts/footer"%>
</div>
</div>
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fa-solid fa-circle-arrow-up"></i>
</a>


             
